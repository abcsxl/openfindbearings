apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: secretstore
spec:
  type: secretstores.env.local
  version: v1
  # 用于本地开发环境的 Secret 管理
  # 生产环境建议使用 HashiCorp Vault 或 Kubernetes Secrets
